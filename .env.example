# Copy this file to `.env` (NOT this file) and put your key there. `.env` is gitignored.
# Never put a real key in THIS file — it is committed to git.
# Cloud model: fast + far better than the local 7B. Get a key at https://console.mistral.ai
MISTRAL_API_KEY=
# Optional others:
# ANTHROPIC_API_KEY=
# OLLAMA_BASE_URL=http://localhost:11434
